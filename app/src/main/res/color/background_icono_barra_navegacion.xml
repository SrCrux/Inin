<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/colorPrimaryLight" android:state_checked="true" android:color="@color/colorPrimaryLight" />  <!-- Fondo del ítem seleccionado -->
    <item android:drawable="@android:color/transparent" android:state_checked="false" android:color="@android:color/transparent" />  <!-- Fondo del ítem no seleccionado (transparente) -->
</selector>