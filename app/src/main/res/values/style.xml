<?xml version="1.0" encoding="utf-8"?>


    <resources>

        <!-- Estilo personalizado para TextInputLayout -->
        <style name="CustomTextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
            <!-- Mantener el tema para el cursor -->
            <item name="android:theme">@style/cursor_theme</item>

            <!-- Personalización del TextInputLayout -->
            <item name="android:layout_width">0dp</item>
            <item name="android:layout_height">wrap_content</item>
            <item name="android:layout_marginTop">44dp</item>
            <item name="android:textColorHint">@color/hint_color</item>
            <item name="boxBackgroundColor">@color/colorPrimaryLight</item>
            <item name="boxBackgroundMode">filled</item>
            <item name="boxStrokeColor">@color/colorPrimaryLight</item>
            <item name="hintTextColor">@color/hint_color</item>
            <item name="boxStrokeWidth">0dp</item>
            <item name="boxStrokeWidthFocused">0dp</item>
            <item name="shapeAppearance">@style/ShapeAppearance.MaterialComponents.SmallComponent</item>
            <item name="errorTextColor">@color/colorSecondary</item>
            <item name="errorIconTint">@color/colorSecondaryDark</item>
        </style>

        <!-- Estilo personalizado para el cursor -->
        <style name="cursor_theme" parent="ThemeOverlay.MaterialComponents.TextInputEditText.FilledBox.Dense">
            <item name="colorControlActivated">@color/colorSecondary</item> <!-- Color del cursor -->
        </style>

    <style name="CustomButtonLayout" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <!-- Personalización del Button -->
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">55dp</item>
        <item name="android:layout_marginTop">25dp</item>
        <item name="android:backgroundTint">@android:color/transparent</item>
        <item name="fontFamily">@font/tw_regular</item>
        <item name="android:textColor">@color/colorSecondary</item>
        <item name="android:textSize">18sp</item>
        <item name="shapeAppearance">@style/ShapeAppearance.Material3.SmallComponent</item>
        <item name="strokeColor">@color/colorSecondary</item>
        <item name="strokeWidth">2dp</item>
        <item name="textAllCaps">false</item>

    </style>

    </resources>


